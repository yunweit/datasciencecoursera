The resulting data set is produced by first loading the original data,
And then merge the training and test set together.
Only the mean and standard deviation for each measurement were extracted.
The resulting data set contains the average grouped by activity and each subject.
